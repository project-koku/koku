# autogenerated pyup.io config file
# see https://pyup.io/docs/configuration/ for all available options

schedule: every week

# only update insecure dependencies
update: insecure

requirements:
  - Pipfile
  - Pipfile.lock
