# GlitchTip triage ignore whitelist
# Issues matching any rule below are classified as ignored_normal and not triaged.
#
# Rule fields (all optional, at least one required):
#   issue_id        - exact GlitchTip issue ID
#   culprit         - exact culprit string
#   metadata.type   - error type from metadata
#   message_contains - list of substrings that must ALL appear in the message
#
rules: []
